<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="water.retrofittest">

    <!--联网的请求-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".SecondMvpActivity" />
        <activity android:name=".ThreeActivity"/>
        <activity android:name=".FourthActivity"/>
        <activity android:name=".autodisponse.AutoDisponseTestActivity"/>

        <activity android:name=".arouter.ArounterActy1"/>
        <activity android:name=".arouter.ArounterActy2"/>

        <activity android:name=".arouter.ArouterUrlActy">


        </activity>
    </application>

</manifest>